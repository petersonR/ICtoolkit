% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expand_scope.R
\name{expand_scope}
\alias{expand_scope}
\title{Expand a base formula with interactions and polynomials}
\usage{
expand_scope(formula, data, k = 1L, poly = 1L)
}
\arguments{
\item{formula}{A formula specifying the response and base (main-effect)
predictors, e.g. \code{y ~ x1 + x2 + g}.}

\item{data}{A data frame used to determine which predictors are numeric
(eligible for polynomials) vs. factor/character.}

\item{k}{Integer. Maximum interaction order among base terms.
\code{k = 1} means no interactions (default), \code{k = 2} means pairwise, etc.}

\item{poly}{Integer. Maximum polynomial degree for numeric predictors.
\code{poly = 1} means no polynomials (default), \code{poly = 2} adds squared
terms, \code{poly = 3} adds squared and cubic, etc.}
}
\value{
A list with components:
\describe{
\item{\code{formula}}{The expanded formula.}
\item{\code{P_index}}{A named list of character vectors grouping term labels
by type: \code{"main"} for base terms, \code{"poly_2"}, \code{"poly_3"}, ... for
polynomial terms, and \code{"interact_2"}, \code{"interact_3"}, ... for
interaction terms.  Only non-empty groups are included.}
}
}
\description{
Given a base formula and data, generates an expanded formula containing
interaction terms (up to order \code{k}) and polynomial terms (up to degree
\code{poly}) for numeric predictors. Returns the expanded formula along with a
\code{P_index} list suitable for use with \code{\link[=compute_rbic]{compute_rbic()}} or \code{\link[=ic_step]{ic_step()}}.
}
\details{
Polynomial terms are added as \code{I(x^2)}, \code{I(x^3)}, etc. for each numeric
predictor in the base formula. Factor predictors are included in
interactions but do not receive polynomial terms. Interactions are formed
among the \strong{base} terms only (polynomials do not interact with each other
or with base terms).
}
\examples{
es <- expand_scope(ies_total ~ age + race + hispanic + female,
                   data = covid_eol, k = 2, poly = 2)
es$formula
es$P_index

# Use directly with ic_step
fit_null <- lm(ies_total ~ 1, data = covid_eol)
ic_step(fit_null, scope = list(lower = ~ 1, upper = es$formula),
        direction = "forward", criterion = "RBIC",
        P_index = es$P_index, trace = 0)

}
\seealso{
\code{\link[=ic_step]{ic_step()}}, \code{\link[=compute_rbic]{compute_rbic()}}
}
