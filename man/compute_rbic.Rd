% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_rbic.R
\name{compute_rbic}
\alias{compute_rbic}
\title{Compute RBIC for model objects}
\usage{
compute_rbic(fit, P_index, gamma = "ebic", ...)
}
\arguments{
\item{fit}{A fitted model object of class \code{lm}, \code{glm}, \code{glmnet}, or
\code{ncvreg}.}

\item{P_index}{A named list of character vectors defining variable groups.
See the P_index format section.}

\item{gamma}{Gamma specification.  See the \code{gamma} argument section.
Default \code{"ebic"}.}

\item{...}{Currently unused; reserved for future use.}
}
\value{
A named numeric scalar (\code{lm}/\code{glm}) or vector (\code{glmnet}/\code{ncvreg})
with the following attributes:
\describe{
\item{\code{fit_class}}{Character. The primary class of \code{fit}.}
\item{\code{k}}{Integer scalar or vector. Total estimated parameters
(including intercept).}
\item{\code{gamma}}{The computed gamma value(s): a scalar or vector for
\code{lm}/\code{glm}; a list (one entry per lambda) for \code{glmnet}/\code{ncvreg}.}
\item{\code{P_index}}{The \code{P_index} supplied by the user.}
\item{\code{criterion}}{\code{"RBIC"}.}
}
}
\description{
Computes the Regularized Bayesian Information Criterion (RBIC), an
extension of EBIC that accounts for structured (grouped) predictors.
RBIC applies group-specific EBIC penalties:
}
\details{
\deqn{\mathrm{RBIC} = \mathrm{BIC} + 2 \sum_{g=1}^{G}
  \gamma_g \log\binom{P_g}{k_g}}

where \code{G} is the number of groups, \code{P_g} is the total number of candidate
predictors in group \code{g}, \code{k_g} is the number selected from group \code{g}, and
\code{gamma_g} is a group-level tuning parameter.
}
\section{P_index format}{

\code{P_index} is a named list whose elements identify which predictors belong
to each group:
\itemize{
\item \strong{\code{lm}/\code{glm}}: character vectors of term labels as returned by
\code{attr(terms(fit), "term.labels")} (e.g. \code{"x1"}, \code{"I(x1^2)"},
\code{"x1:x2"}).
\item \strong{\code{glmnet}}: character vectors matching \code{rownames(fit$beta)}.
\item \strong{\code{ncvreg}}: character vectors matching \code{rownames(fit$beta[-1, ])}.
}
}

\section{The \code{gamma} argument}{

\code{gamma} controls how the group-level tuning parameters are determined.
Accepted values:
\describe{
\item{\code{"ebic"} (default)}{Single gamma based on the \strong{total} number of
candidates \code{P_total = sum(p_1, ..., p_K)}, applied uniformly across
all groups.  Matches the paper's default formula:
\deqn{\gamma = \frac{\log(P_{total}/\sqrt{n})}{\log P_{total}}
      \cdot \mathbf{1}(P_{total} \ge \sqrt{n})}}
\item{\code{"per_group"}}{Separate gamma for each group, each computed from
its own \code{p_k}.  Stronger penalisation of larger groups.}
\item{A numeric scalar}{Fixed gamma applied uniformly to all groups,
e.g. \code{gamma = 0.5}.}
\item{A numeric vector of length \code{K}}{Fixed group-specific gammas.}
\item{A function \code{f(P, k, n)}}{Advanced: called once with the full
vectors of group sizes and selected counts; must return a scalar or
length-\code{K} vector.}
}
}

\examples{
set.seed(1)
n  <- 100
x1 <- rnorm(n); x2 <- rnorm(n); x3 <- rnorm(n)
y  <- 2 * x1 - x2 + rnorm(n)
df <- data.frame(y, x1, x2, x3, x1sq = x1^2, x2sq = x2^2)
fit <- lm(y ~ x1 + x2 + x3 + x1sq + x2sq, data = df)

P_index <- list(main = c("x1", "x2", "x3"), squared = c("x1sq", "x2sq"))

compute_rbic(fit, P_index)                          # default (total-P gamma)
compute_rbic(fit, P_index, gamma = "per_group")     # per-group gammas
compute_rbic(fit, P_index, gamma = 0.5)             # fixed gamma
compute_rbic(fit, P_index, gamma = 0)               # gamma=0 recovers BIC

}
\references{
Chen, J., & Chen, Z. (2008). Extended Bayesian information
criteria for model selection with large model spaces. \emph{Biometrika},
\strong{95}(3), 759–771.
}
\seealso{
\code{\link[=compute_ebic]{compute_ebic()}}, \code{\link[=compute_bic]{compute_bic()}}, \code{\link[=ic_step]{ic_step()}}
}
