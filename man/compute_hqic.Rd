% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_hqic.R
\name{compute_hqic}
\alias{compute_hqic}
\title{Compute HQIC for model objects}
\usage{
compute_hqic(fit, ...)
}
\arguments{
\item{fit}{A fitted model object of class \code{lm}, \code{glm}, \code{glmnet}, or
\code{ncvreg}.}

\item{...}{Currently unused; reserved for future use.}
}
\value{
A named numeric scalar (\code{lm}/\code{glm}) or vector (\code{glmnet}/\code{ncvreg})
with the following attributes:
\describe{
\item{\code{fit_class}}{Character. The primary class of \code{fit}.}
\item{\code{k}}{Integer scalar or vector. Number of estimated parameters.}
\item{\code{criterion}}{\code{"HQIC"}.}
}
For \code{glmnet} and \code{ncvreg} results, a \code{lambda} attribute holds the
corresponding lambda values.
}
\description{
Computes the Hannan-Quinn Information Criterion (HQIC).  HQIC uses a penalty
term that lies between AIC and BIC:
}
\details{
\deqn{\mathrm{HQIC} = -2\,\log L + 2\,k\,\log(\log(n))}

where \code{k} is the number of estimated parameters and \code{n} is the sample size.
HQIC is strongly consistent (like BIC) yet less conservative than BIC for
moderate sample sizes.

For \code{lm} and \code{glm} models the result is a scalar; for \code{glmnet} and \code{ncvreg}
objects the result is a numeric vector of length \code{length(lambda)}, one value
per point on the regularization path.
}
\examples{
fit <- lm(mpg ~ wt + hp, data = mtcars)
compute_hqic(fit)

fit_glm <- glm(am ~ wt + hp, data = mtcars, family = binomial)
compute_hqic(fit_glm)

}
\references{
Hannan, E. J., & Quinn, B. G. (1979). The determination of the
order of an autoregression. \emph{Journal of the Royal Statistical Society:
Series B}, \strong{41}(2), 190--195.
}
\seealso{
\code{\link[=compute_aic]{compute_aic()}}, \code{\link[=compute_bic]{compute_bic()}}, \code{\link[=compute_ebic]{compute_ebic()}}, \code{\link[=ic_step]{ic_step()}}
}
