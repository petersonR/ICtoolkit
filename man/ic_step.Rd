% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ic_step.R
\name{ic_step}
\alias{ic_step}
\title{Stepwise model selection using information criteria}
\usage{
ic_step(
  object,
  scope,
  direction = c("both", "backward", "forward"),
  criterion = c("AIC", "AICc", "BIC", "HQIC", "EBIC", "RBIC", "mBIC", "mBIC2"),
  P = NULL,
  P_index = NULL,
  kappa = 4,
  gamma = "ebic",
  trace = 1L,
  steps = 1000L
)
}
\arguments{
\item{object}{A fitted \code{lm}, \code{glm}, or \code{coxph} object (the starting model).
For \code{coxph}, the response should be a \code{Surv()} object; a null Cox model
can be specified as \code{coxph(Surv(time, status) ~ 1, data = ...)}.}

\item{scope}{A formula or list with \code{lower} and/or \code{upper} elements
specifying the model search space.  See \code{\link[MASS:stepAIC]{MASS::stepAIC()}} for details.}

\item{direction}{One of \code{"both"} (default), \code{"backward"}, or \code{"forward"}.}

\item{criterion}{One of \code{"AIC"} (default), \code{"AICc"}, \code{"BIC"}, \code{"HQIC"},
\code{"EBIC"}, \code{"RBIC"}, \code{"mBIC"}, or \code{"mBIC2"}.}

\item{P}{Integer. Total number of candidate predictors.  Required only
for \code{criterion = "EBIC"}.}

\item{P_index}{Named list of character vectors defining variable groups.
Required only for \code{criterion = "RBIC"}.  See Details.}

\item{kappa}{Positive numeric scalar controlling the prior expected number
of true signals.  Used only for \code{criterion = "mBIC"} or \code{"mBIC2"}.
Default \code{4}.  See \code{\link[=compute_mbic]{compute_mbic()}}.}

\item{gamma}{Gamma specification passed to \code{\link[=compute_ebic]{compute_ebic()}} or
\code{\link[=compute_rbic]{compute_rbic()}}.  Ignored for AIC / AICc / BIC / HQIC / mBIC / mBIC2.
See those functions for accepted values (\code{"ebic"}, \code{"per_group"}, numeric,
or a function).}

\item{trace}{Integer controlling verbosity.  \code{0} = silent, \code{1} = one line
per accepted step (default), \code{2} = all candidates at each step.}

\item{steps}{Maximum number of steps.  Default is \code{1000}.}
}
\value{
The final selected model (same class as \code{object}), with an
additional attribute \code{"step_path"}: a data frame recording each accepted
step with columns \code{step}, \code{action} (the term added/dropped), and the IC
value after that step.
}
\description{
Performs forward, backward, or bidirectional stepwise model selection for
\code{lm}, \code{glm}, and \code{coxph} objects using any of the information criteria
provided by ICtoolkit: AIC, AICc, BIC, HQIC, EBIC, RBIC, mBIC, or mBIC2.
}
\details{
At each step, all single-term additions (forward) and/or deletions
(backward) are evaluated.  The move that gives the greatest IC reduction
is accepted.  Stepping halts when no candidate improves the current IC.
}
\section{Scope}{

\code{scope} follows the same convention as \code{\link[MASS:stepAIC]{MASS::stepAIC()}}:
\itemize{
\item A single formula specifies the \strong{upper} bound (largest allowable model).
\item A list with elements \code{lower} and/or \code{upper} specifies both bounds.
The starting model is always \code{object}; terms in \code{lower} can never be
dropped.
}
}

\section{P_index format for RBIC}{

\code{P_index} is a named list whose elements are character vectors of term
labels \strong{exactly as they appear} in \code{attr(terms(fit), "term.labels")}.
For example, if the upper model contains \code{x1}, \code{I(x1^2)}, and \code{x1:x2},
a valid \code{P_index} might be:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{list(main = "x1", squared = "I(x1^2)", interactions = "x1:x2")
}\if{html}{\out{</div>}}

All terms in the upper-model scope that are not covered by any group in
\code{P_index} receive no additional RBIC penalty (their BIC contribution still
applies).
}

\examples{
## Backward BIC selection
fit_full <- lm(mpg ~ ., data = mtcars)
ic_step(fit_full, direction = "backward", criterion = "BIC")

## Forward EBIC selection
fit_null <- lm(mpg ~ 1, data = mtcars)
fit_full <- lm(mpg ~ ., data = mtcars)
ic_step(fit_null,
        scope     = list(lower = fit_null, upper = fit_full),
        direction = "forward",
        criterion = "EBIC",
        P         = ncol(mtcars) - 1L)

## Forward BIC selection for Cox models
\dontrun{
library(survival)
fit0 <- coxph(Surv(time, status) ~ 1, data = lung)
scope_upper <- Surv(time, status) ~ age + sex + ph.ecog + wt.loss
ic_step(fit0,
        scope     = list(lower = ~ 1, upper = scope_upper),
        direction = "forward",
        criterion = "BIC")
}

}
\seealso{
\code{\link[=compute_aic]{compute_aic()}}, \code{\link[=compute_aicc]{compute_aicc()}}, \code{\link[=compute_bic]{compute_bic()}},
\code{\link[=compute_hqic]{compute_hqic()}}, \code{\link[=compute_ebic]{compute_ebic()}}, \code{\link[=compute_rbic]{compute_rbic()}},
\code{\link[=compute_mbic]{compute_mbic()}}, \code{\link[=compute_mbic2]{compute_mbic2()}}, \code{\link[MASS:stepAIC]{MASS::stepAIC()}}
}
