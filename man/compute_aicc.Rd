% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_aicc.R
\name{compute_aicc}
\alias{compute_aicc}
\title{Compute AICc for model objects}
\usage{
compute_aicc(fit, ...)
}
\arguments{
\item{fit}{A fitted model object of class \code{lm}, \code{glm}, \code{glmnet}, or
\code{ncvreg}.}

\item{...}{Currently unused; reserved for future use.}
}
\value{
A named numeric scalar (\code{lm}/\code{glm}) or vector (\code{glmnet}/\code{ncvreg})
with the following attributes:
\describe{
\item{\code{fit_class}}{Character. The primary class of \code{fit}.}
\item{\code{k}}{Integer scalar or vector. Number of estimated parameters.}
\item{\code{criterion}}{\code{"AICc"}.}
}
For \code{glmnet} and \code{ncvreg} results, a \code{lambda} attribute holds the
corresponding lambda values.  Values for which \code{n <= k + 1} (undefined
correction) are returned as \code{NA}.
}
\description{
Computes the corrected Akaike Information Criterion (AICc), which adjusts
AIC for small sample sizes.  The correction term is \verb{2k(k+1)/(n-k-1)},
where \code{k} is the number of estimated parameters and \code{n} is the sample size.
AICc converges to AIC as \code{n -> infinity}.
}
\details{
For \code{glmnet} with \code{family = "gaussian"}, the log-likelihood is computed
exactly (matching \code{AIC(lm(...))} on the same data).  For other families
the deviance-based approximation is used (see \code{\link[=compute_aic]{compute_aic()}} for details).
}
\examples{
fit <- lm(mpg ~ wt + hp, data = mtcars)
compute_aicc(fit)

fit_glm <- glm(am ~ wt + hp, data = mtcars, family = binomial)
compute_aicc(fit_glm)

}
\references{
Hurvich, C. M., & Tsai, C.-L. (1989). Regression and time
series model selection in small samples. \emph{Biometrika}, \strong{76}(2), 297–307.
}
\seealso{
\code{\link[=compute_aic]{compute_aic()}}, \code{\link[=compute_bic]{compute_bic()}}, \code{\link[=compute_ebic]{compute_ebic()}},
\code{\link[=compute_rbic]{compute_rbic()}}, \code{\link[=ic_step]{ic_step()}}
}
