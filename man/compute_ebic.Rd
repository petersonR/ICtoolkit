% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_ebic.R
\name{compute_ebic}
\alias{compute_ebic}
\title{Compute EBIC for model objects}
\usage{
compute_ebic(fit, P = NULL, gamma = "ebic", ...)
}
\arguments{
\item{fit}{A fitted model object of class \code{lm}, \code{glm}, \code{glmnet}, or
\code{ncvreg}.}

\item{P}{Integer. Total number of \emph{candidate} predictors (excluding the
intercept) considered during model selection.  If \code{NULL} (the default),
inferred from \code{fit} (see Inferring P section).}

\item{gamma}{Controls the sparsity tuning parameter.  One of:
\describe{
\item{\code{"ebic"} (default)}{The data-adaptive rule of Chen & Chen (2008):
\eqn{\gamma = \log(P/\sqrt{n}) / \log P \cdot \mathbf{1}(P \ge \sqrt{n})}.}
\item{A numeric scalar}{Fixed gamma value, e.g. \code{gamma = 0.5}.}
\item{A function \code{f(P, k, n)}}{Custom rule; advanced use only.}
}}

\item{...}{Currently unused; reserved for future use.}
}
\value{
A named numeric scalar (\code{lm}/\code{glm}) or vector (\code{glmnet}/\code{ncvreg})
with the following attributes:
\describe{
\item{\code{fit_class}}{Character. The primary class of \code{fit}.}
\item{\code{k}}{Integer scalar or vector. Number of estimated parameters
(including intercept).}
\item{\code{gamma}}{Numeric scalar or vector. The computed gamma value(s).}
\item{\code{P}}{Integer. The value of \code{P} used.}
\item{\code{criterion}}{\code{"EBIC"}.}
}
}
\description{
Computes the Extended Bayesian Information Criterion (EBIC) of Chen & Chen
(2008).  EBIC adds a sparsity-encouraging penalty to BIC:
}
\details{
\deqn{\mathrm{EBIC} = \mathrm{BIC} + 2\,\gamma\,\log\binom{P}{k}}

where \code{P} is the total number of candidate predictors, \code{k} is the number
of selected predictors (nonzero coefficients, excluding the intercept),
and \code{gamma} is a tuning parameter.

For \code{glmnet} and \code{ncvreg}, EBIC is computed at each point on the
regularization path (one value per lambda).
}
\section{Inferring P}{

If \code{P} is not supplied, it is inferred from the model object:
\describe{
\item{\code{glmnet}}{\code{nrow(fit$beta)} — the number of columns in the
original design matrix \code{X}.}
\item{\code{ncvreg}}{\code{nrow(fit$beta) - 1} — excluding the intercept row.}
\item{\code{lm} / \code{glm}}{\code{length(coef(fit)) - 1} — the number of terms in
the \strong{fitted} model.  This is correct only when the fitted model
contains all candidate predictors (e.g. the full model); if the model
is a subset, you should supply \code{P} explicitly.}
}
}

\examples{
set.seed(1)
X <- matrix(rnorm(100 * 20), 100, 20)
y <- X[, 1:3] \%*\% c(2, -1, 0.5) + rnorm(100)
fit <- lm(y ~ X)

compute_ebic(fit, P = 20)               # explicit P
compute_ebic(fit)                        # infers P = 20 from model
compute_ebic(fit, P = 20, gamma = 0.5)  # fixed gamma
compute_ebic(fit, P = 20, gamma = 0)    # gamma = 0 recovers BIC

}
\references{
Chen, J., & Chen, Z. (2008). Extended Bayesian information
criteria for model selection with large model spaces. \emph{Biometrika},
\strong{95}(3), 759–771.
}
\seealso{
\code{\link[=compute_bic]{compute_bic()}}, \code{\link[=compute_rbic]{compute_rbic()}}, \code{\link[=ic_step]{ic_step()}}
}
