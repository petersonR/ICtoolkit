% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_bic.R
\name{compute_bic}
\alias{compute_bic}
\title{Compute BIC for model objects}
\usage{
compute_bic(fit, ...)
}
\arguments{
\item{fit}{A fitted model object of class \code{lm}, \code{glm}, \code{glmnet}, or
\code{ncvreg}.}

\item{...}{Currently unused; reserved for future use.}
}
\value{
A named numeric scalar (\code{lm}/\code{glm}) or vector (\code{glmnet}/\code{ncvreg})
with the following attributes:
\describe{
\item{\code{fit_class}}{Character. The primary class of \code{fit}.}
\item{\code{k}}{Integer scalar or vector. Number of estimated parameters.}
\item{\code{criterion}}{\code{"BIC"}.}
}
For \code{glmnet} and \code{ncvreg} results, a \code{lambda} attribute holds the
corresponding lambda values.
}
\description{
Computes the Bayesian Information Criterion (BIC), also known as the
Schwarz criterion.  For \code{lm} and \code{glm} models the result is a scalar;
for \code{glmnet} and \code{ncvreg} objects the result is a numeric vector of
length \code{length(lambda)}, one value per point on the regularization path.
}
\details{
For \code{glmnet} with \code{family = "gaussian"}, the log-likelihood is computed
exactly (matching \code{BIC(lm(...))} on the same data).  For other families
the deviance-based approximation is used (see \code{\link[=compute_aic]{compute_aic()}} for details).
}
\examples{
fit <- lm(mpg ~ wt + hp, data = mtcars)
compute_bic(fit)

fit_glm <- glm(am ~ wt + hp, data = mtcars, family = binomial)
compute_bic(fit_glm)

}
\references{
Schwarz, G. (1978). Estimating the dimension of a model.
\emph{The Annals of Statistics}, \strong{6}(2), 461–464.
}
\seealso{
\code{\link[=compute_aic]{compute_aic()}}, \code{\link[=compute_aicc]{compute_aicc()}}, \code{\link[=compute_ebic]{compute_ebic()}},
\code{\link[=compute_rbic]{compute_rbic()}}, \code{\link[=ic_step]{ic_step()}}
}
