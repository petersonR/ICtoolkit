% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_aic.R
\name{compute_aic}
\alias{compute_aic}
\title{Compute AIC for model objects}
\usage{
compute_aic(fit, ...)
}
\arguments{
\item{fit}{A fitted model object of class \code{lm}, \code{glm}, \code{glmnet}, or
\code{ncvreg}.}

\item{...}{Currently unused; reserved for future use.}
}
\value{
A named numeric scalar (\code{lm}/\code{glm}) or vector (\code{glmnet}/\code{ncvreg})
with the following attributes:
\describe{
\item{\code{fit_class}}{Character. The primary class of \code{fit}.}
\item{\code{k}}{Integer scalar or vector. Number of estimated parameters
(including the intercept).}
\item{\code{criterion}}{\code{"AIC"}.}
}
For \code{glmnet} and \code{ncvreg} results, the vector is named by lambda index
and an additional \code{lambda} attribute holds the corresponding lambda values.
}
\description{
Computes the Akaike Information Criterion (AIC) for fitted model objects.
For \code{lm} and \code{glm} models the result is a scalar; for \code{glmnet} and \code{ncvreg}
objects the result is a numeric vector of length \code{length(lambda)},
one value per point on the regularization path.
}
\details{
For \code{glmnet} with \code{family = "gaussian"}, the log-likelihood is computed
exactly (matching \code{AIC(lm(...))} on the same data).  For other families
the deviance-based approximation \code{-deviance/2} is used; this is exact for
\code{binomial} and approximate for \code{poisson} and \code{multinomial}.
}
\examples{
fit <- lm(mpg ~ wt + hp, data = mtcars)
compute_aic(fit)

fit_glm <- glm(am ~ wt + hp, data = mtcars, family = binomial)
compute_aic(fit_glm)

}
\references{
Akaike, H. (1974). A new look at the statistical model
identification. \emph{IEEE Transactions on Automatic Control}, \strong{19}(6),
716–723.
}
\seealso{
\code{\link[=compute_aicc]{compute_aicc()}}, \code{\link[=compute_bic]{compute_bic()}}, \code{\link[=compute_ebic]{compute_ebic()}},
\code{\link[=compute_rbic]{compute_rbic()}}, \code{\link[=ic_step]{ic_step()}}
}
