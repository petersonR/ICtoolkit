% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ic_path.R
\name{plot_ic_path}
\alias{plot_ic_path}
\title{Plot information criterion paths for glmnet or ncvreg objects}
\usage{
plot_ic_path(
  fit,
  criteria = c("AIC", "BIC"),
  P = NULL,
  P_index = NULL,
  gamma = "ebic",
  xvar = c("-log(lambda)", "log(lambda)", "lambda"),
  mark_min = TRUE,
  legend = TRUE,
  cols = NULL,
  lwd = 2,
  ...
)
}
\arguments{
\item{fit}{A fitted \code{glmnet} or \code{ncvreg} object.}

\item{criteria}{Character vector of criteria to plot.  One or more of
\code{"AIC"}, \code{"AICc"}, \code{"BIC"}, \code{"EBIC"}, \code{"RBIC"}.  Default \code{c("AIC", "BIC")}.}

\item{P}{Integer. Total candidate predictors (excluding intercept).
Required when \code{"EBIC"} is in \code{criteria}.}

\item{P_index}{Named list of variable groups.  Required when \code{"RBIC"} is
in \code{criteria}.  See \code{\link[=compute_rbic]{compute_rbic()}} for format details.}

\item{gamma}{Gamma specification for EBIC / RBIC.  Default \code{"ebic"}.
See \code{\link[=compute_ebic]{compute_ebic()}} for accepted values.}

\item{xvar}{What to place on the x-axis.  One of \code{"-log(lambda)"} (default),
\code{"log(lambda)"}, or \code{"lambda"}.}

\item{mark_min}{Logical.  Draw a vertical dashed line at the
IC-minimising lambda for each criterion.  Default \code{TRUE}.}

\item{legend}{Logical.  Draw a legend when more than one criterion is
plotted.  Default \code{TRUE}.}

\item{cols}{Named or unnamed character/integer vector of colours, one per
criterion.  Defaults to \code{seq_along(criteria)} (R's standard palette).}

\item{lwd}{Line width.  Default \code{2}.}

\item{...}{Additional graphical parameters passed to \code{\link[graphics:plot.default]{graphics::plot()}}.}
}
\value{
Invisibly returns a list with:
\describe{
\item{\code{ic}}{Named list of IC vectors, one element per criterion.}
\item{\code{opt_lambda}}{Named numeric vector of IC-minimising lambda values.}
\item{\code{lambda}}{The full lambda sequence from \code{fit}.}
}
}
\description{
Computes one or more information criteria across the full regularisation
path of a \code{glmnet} or \code{ncvreg} object and plots each as a line against
a transform of lambda.  A vertical dashed line marks the lambda that
minimises each criterion.
}
\examples{
\dontrun{
library(glmnet)
set.seed(1)
X <- matrix(rnorm(100 * 20), 100, 20,
            dimnames = list(NULL, paste0("x", 1:20)))
y <- X[, 1] * 2 - X[, 3] + rnorm(100)
fit <- glmnet(X, y)

plot_ic_path(fit, criteria = c("AIC", "BIC", "EBIC"), P = 20)

# RBIC with two predictor groups
P_index <- list(first10 = paste0("x", 1:10), second10 = paste0("x", 11:20))
plot_ic_path(fit, criteria = c("BIC", "RBIC"), P_index = P_index)
}

}
\seealso{
\code{\link[=compute_aic]{compute_aic()}}, \code{\link[=compute_bic]{compute_bic()}}, \code{\link[=compute_ebic]{compute_ebic()}},
\code{\link[=compute_rbic]{compute_rbic()}}
}
